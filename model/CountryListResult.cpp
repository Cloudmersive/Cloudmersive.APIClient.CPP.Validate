/**
 * validateapi
 * The validation APIs help you validate data. Check if an E-mail address is real. Check if a domain is real. Check up on an IP address, and even where it is located. All this and much more is available in the validation API.
 *
 * OpenAPI spec version: v1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator 2.4.11.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */



#include "CountryListResult.h"

namespace io {
namespace swagger {
namespace client {
namespace model {

CountryListResult::CountryListResult()
{
    m_Successful = false;
    m_SuccessfulIsSet = false;
    m_CountriesIsSet = false;
}

CountryListResult::~CountryListResult()
{
}

void CountryListResult::validate()
{
    // TODO: implement validation
}

web::json::value CountryListResult::toJson() const
{
    web::json::value val = web::json::value::object();

    if(m_SuccessfulIsSet)
    {
        val[utility::conversions::to_string_t("Successful")] = ModelBase::toJson(m_Successful);
    }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Countries )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        if(jsonArray.size() > 0)
        {
            val[utility::conversions::to_string_t("Countries")] = web::json::value::array(jsonArray);
        }
    }

    return val;
}

void CountryListResult::fromJson(web::json::value& val)
{
    if(val.has_field(utility::conversions::to_string_t("Successful")))
    {
        web::json::value& fieldValue = val[utility::conversions::to_string_t("Successful")];
        if(!fieldValue.is_null())
        {
            setSuccessful(ModelBase::boolFromJson(fieldValue));
        }
    }
    {
        m_Countries.clear();
        std::vector<web::json::value> jsonArray;
        if(val.has_field(utility::conversions::to_string_t("Countries")))
        {
        for( auto& item : val[utility::conversions::to_string_t("Countries")].as_array() )
        {
            if(item.is_null())
            {
                m_Countries.push_back( std::shared_ptr<CountryDetails>(nullptr) );
            }
            else
            {
                std::shared_ptr<CountryDetails> newItem(new CountryDetails());
                newItem->fromJson(item);
                m_Countries.push_back( newItem );
            }
        }
        }
    }
}

void CountryListResult::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    if(m_SuccessfulIsSet)
    {
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t("Successful"), m_Successful));
    }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Countries )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        
        if(jsonArray.size() > 0)
        {
            multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t("Countries"), web::json::value::array(jsonArray), utility::conversions::to_string_t("application/json")));
        }
    }
}

void CountryListResult::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    if(multipart->hasContent(utility::conversions::to_string_t("Successful")))
    {
        setSuccessful(ModelBase::boolFromHttpContent(multipart->getContent(utility::conversions::to_string_t("Successful"))));
    }
    {
        m_Countries.clear();
        if(multipart->hasContent(utility::conversions::to_string_t("Countries")))
        {

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(utility::conversions::to_string_t("Countries"))));
        for( auto& item : jsonArray.as_array() )
        {
            if(item.is_null())
            {
                m_Countries.push_back( std::shared_ptr<CountryDetails>(nullptr) );
            }
            else
            {
                std::shared_ptr<CountryDetails> newItem(new CountryDetails());
                newItem->fromJson(item);
                m_Countries.push_back( newItem );
            }
        }
        }
    }
}

bool CountryListResult::isSuccessful() const
{
    return m_Successful;
}


void CountryListResult::setSuccessful(bool value)
{
    m_Successful = value;
    m_SuccessfulIsSet = true;
}
bool CountryListResult::successfulIsSet() const
{
    return m_SuccessfulIsSet;
}

void CountryListResult::unsetSuccessful()
{
    m_SuccessfulIsSet = false;
}

std::vector<std::shared_ptr<CountryDetails>>& CountryListResult::getCountries()
{
    return m_Countries;
}

void CountryListResult::setCountries(std::vector<std::shared_ptr<CountryDetails>> value)
{
    m_Countries = value;
    m_CountriesIsSet = true;
}
bool CountryListResult::countriesIsSet() const
{
    return m_CountriesIsSet;
}

void CountryListResult::unsetCountries()
{
    m_CountriesIsSet = false;
}

}
}
}
}

